/**
 * types.ts의 BootAttestation 인터페이스에 다음 필드 추가:
 */

/*
export interface BootAttestation {
  enclaveId: Hash;
  publicKey: Address;
  bootTime: number;
  codeHash: Hash;
  awsAttestationDocument: Bytes;
  signature: Signature;
  // 추가 필드
  pcrs?: {
    [key: string]: string;
  };
  isRealAttestation?: boolean;
}
*/
/**
 * types.ts의 BootAttestation 인터페이스에 다음 필드 추가:
 */

/*
export interface BootAttestation {
  enclaveId: Hash;
  publicKey: Address;
  bootTime: number;
  codeHash: Hash;
  awsAttestationDocument: Bytes;
  signature: Signature;
  // 추가 필드
  pcrs?: {
    [key: string]: string;
  };
  isRealAttestation?: boolean;
}
*/
